

          teoria (tiempo: 20-30 mins, o si quieren mas)

Temas teoricos de la presentacion:

    ...
    v-model, v-bind, v-for, componentes
    !?


          1era parte (tiempo: 10-15 minutos)

Project set up (añadir a la presentacion)

    como instalar nodejs
    inicializar git
    como inicializar npm (explicar que es npm)
    como instalar vue (npm install vue)
    como instalar atom (y adicionalmente buscar plugins de JS y Vue ?!)


          2da parte (tiempo: 35-40 mins)

Curso de laracast

    Commit                                      Tema principal

    Basic data binding                          v-model, {{}}
    lists                                       v-for, v-text
    event listeners                             v-on (con eventos por defecto)
    Attribute and class binding                 v-bind (shorthand para v-bind) (v-bind para atributos de html)
    Computed Properties                         computed
    Components                                  creacion de componentes, <slot>
    Components part 2                           Anidacion de componente, instanciacion de variables en una componente.
    Custom Event listeners                      $emit (emicion de eventos),  v-if   <-- Como utilizar bulma (que es un CDN)


    // todo: Axios, Properties (Adding dynamic behaviour to components), Input types, scoped CSS


Curso de juan

  1er ejercicio

    // todo: ver el ejercicio de juan "Ejercicio de componentes" O el video de laracast commit "Recap"

  Ejercicio final

      // todo: Router.
